# Git LFS configuration for large data files

# Track all CSV files in data directory
data/**/*.csv filter=lfs diff=lfs merge=lfs -text

# Track model files
models/*.pkl filter=lfs diff=lfs merge=lfs -text

# Track output HTML files (visualizations)
outputs/**/*.html filter=lfs diff=lfs merge=lfs -text
